<%= form_for ([@usuario, @livro]) do |f| %>
  <h1> Editar livro </h1><br>
  <p>
    <%= f.label 'Titulo' %><br>
    <%= f.text_field :nome %>
  </p>
  <p>
    <%= f.label :Autor %><br>
    <%= f.text_field :autor %>
  </p>
  <p>
    <%= f.label 'Selecione uma imagem'%>
    <%= f.file_field :imagem %>
  </p>
  <p>
    <%= f.check_box(:concluido) %>
    <%= f.label(:concluido, "Leitura concluída")%>
  </p>
  <p>
    <%= f.submit 'Enviar' %>
  </p>
<% end %>
<%= link_to 'Voltar', usuario_path(@usuario)%>
